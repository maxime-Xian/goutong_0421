# Skills Overview

This file is automatically generated by the `auto-documenter` skill.

| Skill Name | Description |
| :--- | :--- |
| **Agent Browser** | A fast Rust-based headless browser automation CLI with Node.js fallback that enables AI agents to navigate, click, type, and snapshot pages via structured commands. |
| **agent-reach-advisor** | A specialized tool that analyzes agent-reach output for issues (❌, ⚠️) and new versions (🆕), providing specific upgrade commands and remediation advice. Use this when running agent-reach watch or check tasks. |
| **api-connectivity-guard** | A tool for testing the reachability and regional status of various API endpoints (OpenAI, Claude, MiniMax, Feishu, Telegram). Use this to diagnose why a model or channel might be failing. |
| **api-sentinel** | A tool that parses tool outputs for errors (❌, ⚠️) or updates (🆕) and suggests remediation steps. Use when you need to provide clear, actionable feedback to the user after a tool execution. |
| **auto-documenter** | A tool that scans the `skills/` directory and generates a consolidated `SKILLS_OVERVIEW.md` file. Use this to maintain an up-to-date catalog of all installed agent skills. |
| **auto-updater** | No frontmatter found. |
| **capability-evolver** | No frontmatter found. |
| **capability-evolver** | A self-evolution engine for AI agents. Analyzes runtime history to identify improvements and applies protocol-constrained evolution. |
| **clawdchat** | No description provided. |
| **code-stats** | A skill that analyzes code repositories to provide complexity metrics, file counts, and language distribution. Use when you need to understand the structure and health of a codebase. |
| **eca-calibration-monitor** | ECA v2.2 — Monitor FP/FN, cognitive ROI, and drift. Recommend safe updates. |
| **eca-entropy-filter** | ECA v2.2 — Compress raw inputs into a high-signal pack (Top-N / sigma / noise drop). |
| **eca-residual-harvester** | ECA v2.2 — Sample ignored residuals to detect blind spots; route findings to Judgment/Action/Trap. |
| **eca-shadow-deployer** | ECA v2.2 — Shadow deploy updates (thresholds/rules/trap promotions). Promote/Hold/Rollback based on calibration. |
| **eca-signal-gate** | ECA v2.2 — Dual-threshold routing: IGNORE / LIGHT_REVIEW / DEEP_REASON with reason codes. |
| **eca-strategic-reasoner** | ECA v2.2 — Deep reasoning only when route=DEEP_REASON. Produce decision+options+tradeoffs+trace. |
| **eca-trap-detector** | ECA v2.2 — Trap Modules (Warn): detect likely failure-path similarity and output avoidance guidance. |
| **eca-trap-promoter** | ECA v2.2 — Promote high-confidence traps into entropy-layer block/ignore rules (candidate only). |
| **evolution-timeline-viz** | Parses evolution event logs to generate a markdown summary table. Use this to review recent evolutionary progress. |
| **evomap** | Connect to the EvoMap collaborative evolution marketplace. Publish Gene+Capsule bundles, fetch promoted assets, claim bounty tasks, register as a worker, create and express recipes, collaborate in sessions, bid on bounties, resolve disputes, and earn credits via the GEP-A2A protocol. Use when the user mentions EvoMap, evolution assets, A2A protocol, capsule publishing, agent marketplace, worker pool, recipe, organism, session collaboration, or service marketplace. |
| **evomap-capsules** | EvoMap 协议封装技能：快速查询本地任务胶囊和进化方案。 |
| **feishu-common** | No frontmatter found. |
| **feishu-evolver-wrapper** | Feishu-integrated wrapper for the capability-evolver. Manages the evolution loop lifecycle (start/stop/ensure), sends rich Feishu card reports, and provides dashboard visualization. Use when running evolver with Feishu reporting or when managing the evolution daemon. |
| **find-skills** | Helps users discover and install agent skills when they ask questions like "how do I do X", "find a skill for X", "is there a skill that can...", or express interest in extending capabilities. This skill should be used when the user is looking for functionality that might exist as an installable skill. |
| **local-vector-store** | A lightweight local knowledge store that provides simple indexing and retrieval using TF-IDF similarity. Use when you need to store and search through small to medium sets of documents without external dependencies. |
| **maxime-persona** | No description provided. |
| **meme-generator** | A tool for creating memes. It overlays top and bottom text onto a base image using ffmpeg. Use this when you want to create visual content for social engagement or emphasis. |
| **memory-hygiene** | Audit, clean, and optimize Clawdbot's vector memory (LanceDB). Use when memory is bloated with junk, token usage is high from irrelevant auto-recalls, or setting up memory maintenance automation. |
| **openclaw-cost-guard** | Cost-conscious response mode - minimizes token usage by default, expands only when requested. |
| **peekaboo** | Capture and automate macOS UI with the Peekaboo CLI. |
| **qmd** | Local search/indexing CLI (BM25 + vectors + rerank) with MCP mode. |
| **resource-profiler** | A tool that reports system resource metrics like memory usage, CPU load, and uptime. Use this when you suspect performance bottlenecks or want to monitor system health. |
| **self-improvement** | "Captures learnings, errors, and corrections to enable continuous improvement. Use when: (1) A command or operation fails unexpectedly, (2) User corrects Claude ('No, that's wrong...', 'Actually...'), (3) User requests a capability that doesn't exist, (4) An external API or tool fails, (5) Claude realizes its knowledge is outdated or incorrect, (6) A better approach is discovered for a recurring task. Also review learnings before major tasks." |
| **skill-dependency-analyzer** | A tool that scans the skills directory to map how skills require each other. Use this to understand the internal architecture and prevent circular dependencies. |
| **skill-governance** | OpenClaw Cognitive Operating & Skill Governance Kernel |
| **tavily** | AI-optimized web search via Tavily API. Returns concise, relevant results for AI agents. |
| **token-optimizer** | Reduce OpenClaw AI costs by 97%. Haiku model routing, free Ollama heartbeats, prompt caching, and budget controls. Go from $1,500/month to $50/month in 5 minutes. |
| **turix-mac** | Computer Use Agent (CUA) for macOS automation using TuriX. Use when you need to perform visual tasks on the desktop, such as opening apps, clicking buttons, or navigating UIs that don't have a CLI or API. |
| **video-summarizer** | A tool that uses ffmpeg to extract frames from video files at specified intervals, enabling agents to analyze the visual content using image-based models. Use when you need to 'watch' or summarize a video without human intervention. |
