# Maxime Loop-OS V3.0: 完整架构代码与部署协议 (Complete Implementation)

这份文档旨在为全新的 OpenClaw 实例提供“即插即用”的灵魂与大脑架构。它包含了物理文件结构、核心配置文件以及实现认知闭环的 JavaScript 源码。

---

## 1. 物理目录结构 (Standard Workspace Layout)

请在 OpenClaw 工作区根目录下执行以下结构初始化：

```bash
mkdir -p memory/.dreams
mkdir -p memory/B_Ore
mkdir -p knowledge/A_Internalized
mkdir -p knowledge/E_Meta
mkdir -p skills/local-vector-store
mkdir -p error-correction/TYPE_A
mkdir -p logs/archive
```

---

## 2. 核心协议层 (Layer 1: Governance)

### A. SOUL.md (灵魂宪法)
定义 Agent 的人格基线，防止“AI 废话”和“黑箱操作”。
```markdown
# SOUL.md - 核心运行协议
1. 身份：高价值密度认知伙伴，Maxime 的前额叶监护人。
2. 原则：事实优先，证据导向，标注信息来源层级 (L1-L4)。
3. 禁止：严禁未经请求的情感解读、严禁编造动机、严禁长文本堆砌。
4. 透明度：所有工具调用失败必须包含【错误详情】和【可尝试的修复】。
```

### B. USER.md (用户画像与自适应算法)
实现“越用越懂你”的反馈闭环。
```markdown
# USER.md - 用户画像
- 认知特征：视觉化思维者，高模式识别，长文本易疲劳。
- 物理障碍：书写障碍 (Dysgraphia)，词汇提取延迟 (Anomia)。
- 偏好权重表：
| 场景 | 反馈 | 权重 | 更新日期 |
|---|---|---|---|
| 被当黑箱处理 | 不满 | -3 | 2026-03-28 |
| 逻辑链完整 | 满意 | +2 | 2026-03-28 |
```

---

## 3. 认知闭环实现层 (Layer 3: Cognitive Engine)

### A. 本地向量库源码 (skills/local-vector-store/index.js)
实现毫秒级、零成本的长期记忆检索。

```javascript
/**
 * local-vector-store: 实现 A 类知识的物理刻录
 */
const fs = require('fs');
const path = require('path');
const STORE_PATH = path.join(process.cwd(), 'memory/knowledge-store.json');

function ensureStore() {
  if (!fs.existsSync(path.dirname(STORE_PATH))) fs.mkdirSync(path.dirname(STORE_PATH), { recursive: true });
  if (!fs.existsSync(STORE_PATH)) fs.writeFileSync(STORE_PATH, JSON.stringify({ docs: [] }, null, 2));
}

function tokenize(text) { return text.toLowerCase().match(/\w+/g) || []; }

function getTF(tokens) {
  const counts = {};
  tokens.forEach(t => { counts[t] = (counts[t] || 0) + 1; });
  const tf = {};
  for (const t in counts) { tf[t] = counts[t] / tokens.length; }
  return tf;
}

// 核心：添加 A 类知识
function add(id, content, metadata = {}) {
  ensureStore();
  const data = JSON.parse(fs.readFileSync(STORE_PATH, 'utf8'));
  const tokens = tokenize(content);
  const doc = { id, content, tokens, tf: getTF(tokens), metadata, timestamp: Date.now() };
  const idx = data.docs.findIndex(d => d.id === id);
  if (idx !== -1) data.docs[idx] = doc; else data.docs.push(doc);
  fs.writeFileSync(STORE_PATH, JSON.stringify(data, null, 2));
  return doc;
}

// 核心：相似度检索
function search(query, limit = 5) {
  ensureStore();
  const data = JSON.parse(fs.readFileSync(STORE_PATH, 'utf8'));
  const qTokens = tokenize(query);
  const qTf = getTF(qTokens);
  const results = data.docs.map(doc => {
    let score = 0;
    for (const term in qTf) { if (doc.tf[term]) score += qTf[term] * doc.tf[term]; }
    return { id: doc.id, content: doc.content, metadata: doc.metadata, score };
  });
  return results.filter(r => r.score > 0).sort((a, b) => b.score - a.score).slice(0, limit);
}

module.exports = { add, search };
```

---

## 4. 自动化驱动层 (Heartbeat Tasks)

在 `HEARTBEAT.md` 中配置以下 Cron 任务，实现每日自动内化。

```json
{
 "name": "daily-introspection-plus",
 "schedule": { "kind": "cron", "expr": "0 22 * * *", "tz": "Asia/Shanghai" },
 "payload": {
  "kind": "agentTurn",
  "message": "【认知闭环内化指令】\n1. 扫描今日 .dreams/ 缓存和 B_Ore/ 原始日志。\n2. 调用 L3-E 专家模型审计这些碎片，提取核心教训、用户偏好和硬事实。\n3. 调用 local-vector-store.add() 将结论永久刻录进 L3-A 类资产。\n4. 判定是否有触发旧脚本的行为，并记录到 error-correction/。"
 }
}
```

---

## 5. 专家分层协议 (ECA v2.2 Config)

存放在 `knowledge/E_Meta/eca_layers.json`。

```json
{
  "layers": [
    { "id": "L0", "name": "执行者", "threshold": 0.3, "tokens": 500 },
    { "id": "L1", "name": "监护人", "threshold": 0.7, "tokens": 2000 },
    { "id": "L2", "name": "仲裁者", "threshold": 0.9, "tokens": 5000 },
    { "id": "L3", "name": "专家团", "threshold": 0.95, "tokens": 10000 }
  ],
  "experts": {
    "psychology": "负责识别认知偏差与旧脚本",
    "strategy": "负责商业模式 ROI 评估",
    "tech": "负责底层环境故障修复"
  }
}
```

---

## 6. 部署步骤 (Step-by-Step for New Agent)

1. **注入灵魂**：将 `SOUL.md` 和 `USER.md` 拷贝到工作区根目录。
2. **挂载硬盘**：在 `skills/` 下部署 `local-vector-store` 源码。
3. **建立心跳**：将 JSON 配置填入 `HEARTBEAT.md`，确保每晚 22:00 自动内化。
4. **拉取资产**：通过 Git 将远程仓库的 `knowledge/` 目录拉取至本地。
5. **首次唤醒**：告知新 Agent：“请读取 `Maxime_Loop_OS_Implementation.md`，你现在的使命是遵循 A-E 漏斗协议，并配合 local-vector-store 进行思考。”

---
*Generated by Maxime's Guardian System | V3.0 Standard Implementation*
