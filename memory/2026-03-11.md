# Durable Memories - 2026-03-11

## Control UI Connection Issue
-   **Problem**: Control UI was not auto-connecting despite attempts to configure `controlUi` section in `openclaw.json`.
-   **Discovery**: `controlUi` is not a valid configuration key in OpenClaw.
-   **Solution**: Control UI can be connected by passing the gateway token as a URL parameter: `http://127.0.0.1:18789/?token=oc-admin-8f7d3e9a2b1c5f4e6d0a8c7b3e2f1d9a0b4c6e8d5f2a1b7c3e9d0f8a2b4c6d1e`. User was advised to bookmark this URL.

## ClawdChat Activities
-   **Heartbeats**: Performed multiple ClawdChat heartbeat checks throughout the day.
-   **Engagement**: Browsed new posts, identified interesting discussions, and commented on a total of 9 posts (3 per heartbeat run).
-   **API Issue (Upvotes)**: Consistently received "Not Found" (`404`) errors when attempting to upvote posts, indicating a potential issue with the upvote API endpoint or its usage. This needs further investigation if upvoting becomes a critical task.
-   **Account Deletion**: User requested to delete their ClawdChat account. Confirmed that this is a manual process via the ClawdChat website (not available through API). Offered to remove local credentials and stop automation.

## Control UI "Token Missing" Persistence Issue
-   **Problem**: Despite using the URL parameter, the Control UI consistently showed "token missing" upon reopening, indicating a lack of token persistence.
-   **Attempted Solutions (Failed)**:
    -   Web search via `web_search` tool failed due to `missing_brave_api_key`, even when `provider="tavily"` was specified. This suggests an internal tool configuration issue or `web_search` not correctly routing to Tavily.
    -   Direct semantic search on EvoMap via `curl` failed with a 502 error.
-   **Current Proposed Solution**: Instructed the user to manually navigate to Control UI "Settings" and paste the `oc-admin-8f7d3e9a2b1c5f4e6d0a8c7b3e2f1d9a0b4c6e8d5f2a1b7c3e9d0f8a2b4c6d1e` token into the designated input field, then save and refresh. This aims to resolve the persistence issue within the UI itself.