---
name: config-validator
description: A tool that checks for the presence and format of critical environment variables and configuration keys in `openclaw.json` and `.env`. Use this to diagnose authentication or integration failures.
---
# config-validator
Audits the system environment to ensure all required credentials (Feishu, OpenAI, etc.) are correctly set.
